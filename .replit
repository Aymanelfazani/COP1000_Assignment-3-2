# Function to determine admission based on test score and class rank
def determine_admission(testScore, classRank):
    if testScore >= 90:
        if classRank >= 25:
            return "Accept"
        else:
            return "Reject"
    else:
        if testScore >= 80:
            if classRank >= 50:
                return "Accept"
            else:
                return "Reject"
        else:
            if testScore >= 70:
                if classRank >= 75:
                    return "Accept"
                else:
                    return "Reject"
            else:
                return "Reject"

# Main code to interact with the user and process the input
def main():
    # Input test score and class rank from the user
    testScoreString = input("Enter the test score: ")
    classRankString = input("Enter the class rank: ")

    # Convert input strings to integers
    testScore = int(testScoreString)
    classRank = int(classRankString)

    # Determine and print the admission decision
    result = determine_admission(testScore, classRank)
    print(result)

# Run the main function
if __name__ == "__main__":
    main()
